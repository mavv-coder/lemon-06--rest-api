import * as React from 'react';
import { AppLayout } from 'layouts';
import { DashboardContainer } from '../pods/dashboard';

export const DashboardScene = () => (
  <AppLayout>
    <DashboardContainer />
  </AppLayout>
);
